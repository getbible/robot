# Direct runtime dependencies. Compile with hashes before deployment.
getbible>=2.0.0,<3
# Imported directly for Unicode grapheme segmentation, so search-term
# highlighting can compose and fold exactly the way Librarian analyses.
regex>=2024.11,<2027
# Keep these compatibility dependencies explicit so the Python 3.14 canonical
# lock also contains dependencies selected only on older supported interpreters.
exceptiongroup==1.3.1
typing-extensions==4.16.0
python-dotenv==1.2.2
python-telegram-bot[webhooks]==22.8
requests==2.34.2
# Imported directly by the same-process Telegram Mini App HTTP adapter.
tornado==6.5.7
